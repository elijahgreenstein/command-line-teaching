.. highlight:: none

Installation
============================================================

Command Line Teaching
------------------------------------------------------------

Open a terminal. Type the command below and press ``enter`` to install the CLT toolkit with ``pip``.

.. code-block:: bash

  pip install git+https://github.com/elijahgreenstein/command-line-teaching.git

``pip`` will also install these required Python packages and their dependencies:

* ``jinja2`` (`Jinja <jinja_>`_)
* ``markdown-it-py`` (`Markdown-It-Py <markdown_it_py_>`_)
* ``mdit_py_plugins`` (`Markdown-It-Py Plugin Extensions <mdit_py_plugins_>`_)
* ``pyyaml`` (`PyYAML <pyyaml_>`_)
* ``requests`` (`Requests <requests_>`_)

Next steps
------------------------------------------------------------

Once you have installed CLT, go to the :doc:`next part of the tutorial <course>`.

.. include:: ../links.rst

